// import type { StockInfo } from "../models/companyOverview/Ticker";


// export function filterStocksBy(stocks: [string, StockInfo][], symbol:string, filterBy:StockInfo, searchQuery:string ):any{
    
//     stocks.filter(([symbol, s]) => {
//     return (
//       symbol.toLowerCase().includes(searchQuery.toLowerCase()) ||
//       filterBy[0].toLowerCase().includes(searchQuery.toLowerCase())
//       // name
//     );
//   });
// }